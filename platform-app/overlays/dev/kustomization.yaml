apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dev

namePrefix: platform-
commonLabels:
    instance: platform

bases:
- ../../../common-base

images:
    - name: virtocommerce/platform
      newName: leonidn/platform
      newTag: dev-linux-experimental