apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cms-content-volume
  namespace: test
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 200Mi
  storageClassName: rook-cephfs