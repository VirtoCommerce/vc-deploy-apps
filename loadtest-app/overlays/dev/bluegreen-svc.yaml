apiVersion: v1
kind: Service
metadata:
  name: bluegreen-platform
  labels:
    k8s-app: loadtest-platform
spec:
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    k8s-app: loadtest-platform