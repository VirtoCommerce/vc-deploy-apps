apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cms-ceph
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: rook-cephfs
  dataSource:
    name: xapi-cms-content-volume
    namespace: qa
    kind: PersistentVolumeClaim