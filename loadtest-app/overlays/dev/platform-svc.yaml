apiVersion: v1
kind: Service
metadata:
  name: platform-svc
  # annotations:
  #   linkerd.io/inject: enabled
  labels:
    k8s-app: loadtest-platform
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    k8s-app: loadtest-platform