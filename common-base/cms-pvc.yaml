kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: cms-content-volume
  annotations:
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Mi
  storageClassName: "rook-nfs-share-delete"