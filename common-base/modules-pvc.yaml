apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: modules-volume
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: topolvm-provisioner
  resources:
    requests:
      storage: 200Mi