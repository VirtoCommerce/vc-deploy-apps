apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-{{ .Values.platform.service.name }}"
  labels:
    release: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.platform.service.externalPort }}
  selector:
    app: {{ .Values.platform.service.name }} 