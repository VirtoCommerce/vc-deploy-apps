---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: storefront-policy
  labels:
    app: storefront
spec:
  podSelector:
    matchLabels:
        app: storefront
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - ipBlock:
        10.0.0.0/24
    - namespaceSelector:
        matchLabels:
            instance: sandbox
    - podSelector:
        matchLabels:
          app: storefront
          instance: sandbox
    ports:
    - protocol: TCP
      port: 80      
  egress:
  - {}