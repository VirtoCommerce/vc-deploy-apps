apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-{{ .Values.storefront.service.name }}"
  release: {{ $.Release.Name | quote }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.storefront.service.externalPort }}
  selector:
    app: {{ .Values.storefront.service.name }}  