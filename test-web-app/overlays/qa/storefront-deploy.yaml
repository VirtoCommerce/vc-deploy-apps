apiVersion: apps/v1
kind: Deployment
metadata:
  name: storefront
spec:  
  template:
    spec:
      containers:
      - name: vc-storefront-web        
        env:
          - name: CookieAuthenticationOptions__ExpireTimeSpan
            value: 12:00:00
