apiVersion: v1
kind: PersistentVolume
metadata:
  name: cms-pv
spec:
  capacity:
    storage: 200Mi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: "rook-nfs-share-retain"